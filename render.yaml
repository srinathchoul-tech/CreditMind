services:
  - type: web
    name: creditmind
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: ".\.venv\Scripts\python.exe -m streamlit run main.py --server.port $PORT --server.address 0.0.0.0"